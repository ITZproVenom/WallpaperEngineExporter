# LumaDeck

Clean-room rebuild from the Wallpaper Engine discovery/export idea. New name, bundle, UI, icon and implementation. Public Steam Workshop discovery, local media import, MP4 export and persistent export history.